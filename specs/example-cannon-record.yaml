---
kind: snops/storage/v1

id: base
name: base-ledger

generate: {}

---
kind: snops/cannon/v1

name: committee-tx-public

source:
  query: "*/*" # node targets
  labels: [local] # compute labels

sink:
  file-name: txs.json
